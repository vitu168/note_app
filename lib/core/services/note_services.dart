import 'package:note_app/core/models/note_info.dart';

class NoteServices {
  //Read all note from API
  Future<List<NoteInfo>> getAllNotes() async {

    await Future.delayed(Duration(seconds: 1));
    return [
      
    ];
  }
}
